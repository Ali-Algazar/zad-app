abstract class MyTasksLocalDataSource {
  // Future<void> cacheMyTasks(MyTasksModel model);
}

class MyTasksLocalDataSourceImpl implements MyTasksLocalDataSource {
  // final HiveHelper hiveHelper;
  // MyTasksLocalDataSourceImpl({required this.hiveHelper});
}
